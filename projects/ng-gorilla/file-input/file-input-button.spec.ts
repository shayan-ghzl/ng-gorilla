import { GrlFileInputButton } from "./file-input-button";

describe('grlFileInputButton', () => {
  it('should create an instance', () => {
    const directive = new GrlFileInputButton();
    expect(directive).toBeTruthy();
  });
});
